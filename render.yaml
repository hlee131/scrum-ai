services:
  - type: web
    name: scrum-ai-api
    env: python
    buildCommand: cd server && pip install -r requirements.txt
    startCommand: cd server && gunicorn server:app
    envVars:
      - key: OCTOAI_API_KEY
        sync: false